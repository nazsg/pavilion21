export const state = () => ({
  starters : 'initial data from main-menu.js'
})

export const getters = {
  getStarters: (state) => {
    return state.starters + ' -- from main_menu.js'
  }
}

export const mutations = () => {
  set_test(state, c)
}

